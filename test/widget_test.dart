import 'package:flutter_test/flutter_test.dart';
import 'package:yadd/app/app.dart';

void main() {
  testWidgets('YAAD App Smoke Test', (WidgetTester tester) async {
    // Basic initialization test placeholder
    expect(YaadApp, isNotNull);
  });
}
